import 'package:flutter/cupertino.dart';

class DetailedTaskPage extends StatelessWidget {
  const DetailedTaskPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
